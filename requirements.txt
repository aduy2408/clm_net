# Versions captured from the local ml2 environment.
# For torch/torchvision +cu121 wheels, install with the matching PyTorch CUDA index if pip cannot resolve them.
torch==2.5.1+cu121
torchvision==0.20.1+cu121
natten==0.17.5
albumentations==2.0.8
opencv-python==4.13.0
numpy==2.2.6
scipy==1.15.3
timm==1.0.24
tqdm==4.67.3
pillow==12.0.0
